# Ready-to-submit entry for https://github.com/awesome-dsh-plugin/awesome-dsh-plugin
# Open a PR that adds this file to the list repo as:
#   data/plugins/vitas__dsh-web-search-openrouter.yml
# (READMEs in that repo are generated from data/plugins/*.yml — do not edit them.)
url: https://github.com/vitas/dsh-web-search-openrouter
name: vitas/dsh-web-search-openrouter
category: web
description:
  en: 'Grounded web search for DSH on an OpenRouter-compatible gateway: the built-in web_search tool runs server-side on the same endpoint and key as your chat models, with three request protocols, every citation shape parsed, and an editable settings card with a live connection test.'
  zh: '让 DSH 内置 web_search 工具走 OpenRouter 兼容网关：与对话模型共用端点和密钥，支持三种请求协议、解析全部引用格式，并提供可随时编辑、带实时连接测试的设置卡片。'
