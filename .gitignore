# deps
node_modules/
.pnpm-store/

# build output is committed (lib/client.js) — keep the rest out
*.tsbuildinfo

# local/runtime
.cache/
*.log
.DS_Store

# editor
.idea/
.vscode/

# personal announcement copy (lives on the personal site, not in the package)
# same convention as dsh-model-pricing
docs/announcements.md

# local scratch used by the live smoke test
.env
.env.local
